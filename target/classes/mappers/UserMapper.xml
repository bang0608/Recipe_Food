<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="UserMapper">

<insert id="register">
insert into USER ( id, pw, address, tel)
values (#{id}, #{pw}, #{address}, #{tel})
</insert>


</mapper>